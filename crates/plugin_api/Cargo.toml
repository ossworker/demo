[package]
authors.workspace = true
edition.workspace = true
license.workspace = true
name = "plugin_api"
version.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# wit-bindgen-rt = {version = "0.34", features = ["bitflags"]}
wit-bindgen = { version = "0.35" }

[build-dependencies]
wit-deps = "0.4"
